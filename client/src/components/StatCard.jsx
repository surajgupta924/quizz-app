import { motion } from 'framer-motion';
export default function StatCard({ icon: Icon, label, value, tint = 'from-primary-500 to-violet-400', note }) { return <motion.div whileHover={{ y:-4 }} className="card overflow-hidden"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-3xl font-extrabold tracking-tight">{value ?? '—'}</p>{note && <p className="mt-2 text-xs text-emerald-500">{note}</p>}</div><span className={`grid h-13 w-13 place-items-center rounded-2xl bg-gradient-to-br ${tint} p-3 text-2xl text-white shadow-lg`}><Icon/></span></div></motion.div>; }
