import { useEffect,useState } from 'react'; import api,{notifyError} from '../../services/api'; import PageHeader from '../../components/PageHeader'; import EmptyState from '../../components/EmptyState';
export default function Students(){const [items,setItems]=useState([]),[search,setSearch]=useState('');useEffect(()=>{const t=setTimeout(()=>api.get(`/admin/students?limit=100&search=${encodeURIComponent(search)}`).then(r=>setItems(r.data.items)).catch(notifyError),250);return()=>clearTimeout(t)},[search]);return <><PageHeader eyebrow="People" title="Student management" text="Search and review registered learners."/><div className="card"><input value={search} onChange={e=>setSearch(e.target.value)} className="field mb-5 max-w-md" placeholder="Search name, email, or mobile…"/>{items.length?<div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="text-xs uppercase text-slate-400"><tr><th className="pb-3">Student</th><th>College</th><th>Course</th><th>Mobile</th><th>Joined</th></tr></thead><tbody>{items.map(s=><tr key={s._id} className="border-t"><td className="py-4"><b>{s.name}</b><p className="text-xs text-slate-400">{s.email}</p></td><td>{s.college||'—'}</td><td>{s.course||'—'} {s.year&&`· ${s.year}`}</td><td>{s.mobile}</td><td>{new Date(s.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<EmptyState title="No students found"/>}</div></>}
